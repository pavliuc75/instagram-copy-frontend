#name: Push to Azure Container Registry
#on:
#  push:
#    branches:
#      - main
#
#jobs:
#  build-and-push:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Check out code
#        uses: actions/checkout@v2
#
#      # Set up Docker and login to Azure Container Registry
#      - name: Set up Docker and login
#        uses: azure/docker-login@v1
#        with:
#          login-server: instagram0copy0acr.azurecr.io
#          username: ${{ secrets.REGISTRY_USERNAME }}
#          password: ${{ secrets.REGISTRY_PASSWORD }}
#
#      # Build and tag Docker image
#      - name: Build and tag Docker image
#        run: docker build -t instagram0copy0acr.azurecr.io/instagram-copy-frontend:${{ github.sha }} .
#
#      # Push Docker image to Azure Container Registry
#      - name: Push Docker image to Azure Container Registry
#        run: docker push instagram0copy0acr.azurecr.io/instagram-copy-frontend:${{ github.sha }}
#
#

#name: Deploy to Azure Container Instances
#
#on:
#  push:
#    branches:
#      - main
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v2
#
#      - name: Login to Azure
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
#
#      - name: Build and push Docker image
#        run: |
#          docker build -t instagram0copy0acr.azurecr.io/instagram-copy-frontend:${{ github.sha }} .
#          docker push instagram0copy0acr.azurecr.io/instagram-copy-frontend:${{ github.sha }}
#
#      - name: Update Azure Container Instance
#        uses: azure/aci-deploy@v1
#        with:
#          resource-group: instagram-copy
##          dns-name-label: <dns-name-label>
#          image: instagram0copy0acr.azurecr.io/instagram-copy-frontend:${{ github.sha }}
#          registry-login-server: instagram0copy0acr.azurecr.io
#          registry-username: ${{ secrets.REGISTRY_USERNAME }}
#          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
#          container-name: instagram-copy-frontend-aci
#          container-group-name: instagram-copy-aci-group
#          os-type: Linux
#

name: Deploy to Azure Container Instances
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # checkout the repo
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Login via Azure CLI'
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: 'Build and push image'
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.REGISTRY_LOGIN_SERVER }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - run: |
          docker build . -t ${{ secrets.REGISTRY_LOGIN_SERVER }}/instagram-copy-frontend:${{ github.sha }}
          docker push ${{ secrets.REGISTRY_LOGIN_SERVER }}/instagram-copy-frontend:${{ github.sha }}

      - name: 'Deploy to Azure Container Instances'
        uses: 'azure/aci-deploy@v1'
        with:
          resource-group: ${{ secrets.RESOURCE_GROUP }}
          dns-name-label: ${{ secrets.RESOURCE_GROUP }}${{ github.run_number }}
          image: ${{ secrets.REGISTRY_LOGIN_SERVER }}/instagram-copy-frontend:${{ github.sha }}
          registry-login-server: ${{ secrets.REGISTRY_LOGIN_SERVER }}
          registry-username: ${{ secrets.REGISTRY_USERNAME }}
          registry-password: ${{ secrets.REGISTRY_PASSWORD }}
          name: instagram-copy-frontend-aci
          location: 'norwayeast'